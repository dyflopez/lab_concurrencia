name: Encolar commits

on:
  push:

jobs:
  enqueue:
    runs-on: ubuntu-latest
    steps:
      - name: Crear issue para commit
        uses: peter-evans/create-issue@v4  # 🔧 corregido
        with:
          title: "Deploy pendiente: ${{ github.sha }}"
          body: |
            Commit: `${{ github.sha }}`
            Autor: `${{ github.actor }}`
          labels: queue
